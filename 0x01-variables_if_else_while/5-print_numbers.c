#include <stdio.h>

/**
 * main - The program's entry point
 *
 * Return: Always returns 0 to signify success
 */
int main(void)
{
    int digit;

    for (digit = 0; digit < 10; digit++)
    {
        printf("%d\n", digit);
    }

    return (0);
}

